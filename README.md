# T-FLIPFLOP-
T Flip-Flop GitHub Description  A T (Toggle) Flip-Flop is a sequential logic circuit used to store one bit of data. It has a single T input and toggles its output on every active clock edge when T = 1, while the output remains unchanged when T = 0.
